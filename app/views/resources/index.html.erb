<%= tag.div(flash[:alert], class: "flash severe") if flash[:alert] %>
<%= tag.div(flash[:notice], class: "flash success") if flash[:notice] %>

<h2 class="mt-1.5"><%= t("models.resources") %></h2>
<% if staff? && params[:admin] %>
  <div class="my-.5">
    <%= link_to t("resources.new"), new_admin_resource_path(@resource) %>
  </div>
<% end %>
<!-- Adapted from https://codepen.io/wikyware-net/pen/dyKPRxQ -->
<%= render "filter" %>
<div class="resources-container">
  <%= render @resources %>
</div>
